---
import RootThemeSelect from '@/components/root/ThemeSelect.astro';
import GeoThemeSelect from '@/components/geo/ThemeSelect.astro';

const { pathname } = Astro.url;
---

{['/geo', '/countries'].some(path => pathname.includes(path)) ? (
  <GeoThemeSelect /> 
) : (
  <RootThemeSelect /> 
)}
